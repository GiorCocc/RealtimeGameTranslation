# ── Screen Capture ────────────────────────────────────────────────
dxcam                    # DXGI Desktop Duplication API — bassa latenza, 60+ fps

# ── Computer Vision ───────────────────────────────────────────────
numpy
pillow
opencv-python

# ── OCR ───────────────────────────────────────────────────────────
# Scegliere l'esecuzione adatta:
onnxruntime              # Sostituisce onnxruntime-directml per avere prestazioni eccellenti su CPU
rapidocr-onnxruntime     # Drop-in replacement per PaddleOCR, usa ONNX Runtime
easyocr                  # Fallback per testi su texture / font irregolari

# ── Traduzione locale ─────────────────────────────────────────────
transformers             # Tokenizer MarianMT
sentencepiece            # Richiesto da transformers per MarianMT
ctranslate2              # Motore di inferenza ottimizzato
# torch già richiesto da paddlepaddle, ma elencato esplicitamente:
torch                    # CPU — usare torch cu* per CUDA (vedi pytorch.org)

# ── UI / Overlay ──────────────────────────────────────────────────
PyQt6                    # Finestra trasparente, always-on-top, segnali/slot
pywin32                  # WS_EX_TRANSPARENT click-through su Windows

# ── UX ────────────────────────────────────────────────────────────
keyboard                 # Hotkey globali (F9/F10) senza focus sull'app
pystray                  # Icona system tray
pillow                   # Richiesto da pystray per l'icona

# ── Settings panel (Phase 6) ──────────────────────────────────────
psutil                   # Monitor risorse: CPU % e RAM nel pannello Impostazioni
# wmi                    # Opzionale: nome scheda video nel pannello Risorse (Windows,
                         # "pip install wmi"). Nessuna % utilizzo GPU per AMD senza ROCm.

# ── Other ────────────────────────────────────────────────────────────
sacremoses